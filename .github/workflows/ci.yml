name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  fmt:
    name: Format Check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup OpenTofu
        uses: opentofu/setup-opentofu@a1320f892987e89d278cc92dc5adc984fb93aca4 # v2.0.2
        with:
          tofu_version: "1.8"
          tofu_wrapper: false

      - name: Format Check
        run: tofu fmt -check -recursive -diff

  validate:
    name: Validate
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup OpenTofu
        uses: opentofu/setup-opentofu@a1320f892987e89d278cc92dc5adc984fb93aca4 # v2.0.2
        with:
          tofu_version: "1.8"
          tofu_wrapper: false

      - name: Initialize
        run: tofu init -backend=false

      - name: Validate
        run: tofu validate

  tflint:
    name: TFLint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup TFLint
        uses: terraform-linters/setup-tflint@6e1e0642c0289bd619021bf6b34e3c08ed1e005a # v6.3.0
        with:
          tflint_version: latest

      - name: Initialize TFLint
        run: tflint --init
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Run TFLint
        run: tflint --recursive --format compact

  docs:
    name: Documentation Check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup terraform-docs
        uses: terraform-docs/gh-actions@6de6da0cefcc6b4b7a5cbea4d79d97060733093c # v1.4.1
        with:
          working-dir: .
          output-file: README.md
          output-method: inject
          # Docs are generated in-place here. For human-authored PRs the check
          # fails on a stale README (matching the lefthook pre-commit gate). For
          # Dependabot PRs it does not fail: Dependabot can't run the local
          # lefthook hook, and a follow-up bot commit to refresh the README is
          # pushed with a token that does not re-trigger required checks, which
          # left the PR's head SHA permanently uncovered and BLOCKED. The README
          # requirements table is refreshed on the next human commit (enforced
          # by lefthook). See removed dependabot-docs.yml workflow.
          #
          # Only gate human-authored pull requests. On push to main the code is
          # already merged, so a stale README (e.g. right after a Dependabot
          # auto-merge) can't be fixed by failing here -- it just leaves main
          # red until the next human commit. The push-to-main actor is the
          # auto-merge bot, not dependabot[bot], so the actor check alone does
          # not suppress it.
          fail-on-diff: ${{ github.event_name == 'pull_request' && github.actor != 'dependabot[bot]' }}

  security:
    name: Security Scan
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Run Trivy
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          scan-type: 'config'
          scan-ref: '.'
          trivy-config: trivy.yaml
          exit-code: '1'
          severity: 'HIGH,CRITICAL'

  validate-examples:
    name: Validate Examples
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        example:
          - basic
          - vpc
          - advanced-iam
          - multi-lambda
          - api-gateway
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup OpenTofu
        uses: opentofu/setup-opentofu@a1320f892987e89d278cc92dc5adc984fb93aca4 # v2.0.2
        with:
          tofu_version: "1.8"
          tofu_wrapper: false

      - name: Initialize Example
        working-directory: examples/${{ matrix.example }}
        run: tofu init -backend=false

      - name: Validate Example
        working-directory: examples/${{ matrix.example }}
        run: tofu validate
